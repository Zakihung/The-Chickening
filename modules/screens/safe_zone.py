class SafeZone:
    def __init__(self, screen, player):
        self.screen = screen
        self.player = player
        self.buttons = [{'text': 'Store Thóc', 'rect': ..., 'action': 'store'}]
        # Add shop/skills

    def update(self, events, action=None):
        # Handle click store player.store_thoc()
        return action or None

    def draw(self):
        return
        # Draw safe zone bg/buttons